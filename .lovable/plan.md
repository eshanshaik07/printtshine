# Add an empty cart page

## What will change
- Add a shopping-cart icon to the desktop and mobile navigation controls.
- Make the icon open a dedicated Cart page.
- Show a polished empty-cart state with a clear button that takes visitors to Works.
- Keep the existing navigation layout, animations, theme support, and mobile menu behavior.

## Technical details
- Create a `/cart` route with unique page metadata.
- Use TanStack `Link` navigation for both the cart icon and the Works action.
- Update the task roadmap and verify the page at desktop and mobile sizes.
